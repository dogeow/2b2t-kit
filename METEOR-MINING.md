# 挖掘与 Meteor 自动吃 / 自动工具

## 引擎 1.7.13（源码主包版本 1.8.17）

- 范围：盾构机全部挖掘模式，包括区域挖、自动找矿及沿路回程。不更改界面、Mixin、宿主 API 或 Meteor 配置。
- 对照实际安装的 `meteor-client-26.1.2-42.jar` 核实了 `AutoEat.shouldEat()`、`eating`、`Module.settings` 和防破计算；相邻 26.2 源码只作为参考。
- 本次读取的 Meteor 存盘配置启用了 AutoEat 和 AutoTool，AutoTool 开启 anti-break，未存储自定义防破百分比。运行时从模块对象读取当前值，不把磁盘默认值写死到引擎。

## 进食优先

- 每拍在区域导航、战斗和选工具前调用 Meteor 原生进食判断，沿用其阈值模式、血量/饱食度、黑名单和食物搜索范围。不自行选择、消耗食物，不改阈值。
- 需要吃、正在吃或已经使用食物时，暂停挖掘/移动/视角，不再反复松开右键或换回镐。区域挖保存当前位置进度并悬停；若身处液体则暂停挖掘并上浮。
- 暂停拉弓时不提前恢复工具槽或攻击模块，避免抢走食物；连续两拍确认进食结束后才恢复挖掘。吃多份食物之间的短间隔不恢复。
- 暂停期间不累计挖掘或移动卡住计时。连续 200 客户端 tick 未补充血量或饱食度时，停止挖掘并提示检查食物、快捷栏空位或网络，不继续空等。普通情况下吃完自动续挖，无需再次按开始。
- 血量过低等既有紧急保护仍然有效，不为了进食关闭安全下线。

## 工具防破

- 选镐与离线判断共用实时 AutoTool 防破百分比、黑白名单，并保留工具箱原有剩余 8 点耐久下限。
- Meteor 的百分比计算是整数除法，剩余耐久 **小于** `最大耐久 × 百分比 / 100` 时被保护；例如钻石镐 10% 门槛为 156，剩余 155 不再使用，156 尚可使用。
- 扫描快捷栏、主背包和副手。先换出健康且适合当前方块的备用工具；需正确工具才能掉落的方块不使用错误等级工具空挖。
- 所有镐均受保护或被用户工具列表排除时，立即停挖、保存进度并排队安全下线，不再尝试挖回家路线。完好的斧头、铲子不能掩盖镐已全部不可用。
- 若没有符合当前方块的工具或换出未确认，停止并提示补充/检查，不授权攻击。Meteor 设置读取失败也停止，不通过忽略防破继续挖。

## 验证与部署

- JDK 25 离线 `test jar verifyRuntimeEngineJar` 通过；383 项测试，0 失败，新增 25 项：原生进食判断与动态设置、跨食物间隔、进食超时、工具精确门槛、背包与副手备用镐、工具列表、控制分支与排队下线接线。
- 只部署 `config/twob2tkit/runtime/twob2tkit-engine.jar` 1.7.13；主包 1.8.16 和 API 2 不变。旧引擎备份在 `config/twob2tkit/backups/2026-09-06-eat-tools/`。
- 本次更改可用 `/twob2tkit reload` 热加载。检查时最新日志仍是主包 1.8.11、加载快照 1.7.11；上一轮 1.8.16 主包崩溃保护必须完整重启后才生效，参见 `CRASH-2026-09-06.md`。
- 尚未在服务器里实测进食和最后一把镐的下线。实测时关注 `meteor-eat-pause/resume/blocked`、`tool-switch`、`tools-protected-logout`；需要确认恢复后仍在原井、工具耐久与 Meteor 门槛一致、断线不崩溃。
