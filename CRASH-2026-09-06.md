# 低血自动下线后的 KillAura 崩溃

## 已确认的调用链

用户导出目录：`/Applications/minecraft-exported-crash-info-2026-09-06T22-33-52/`。未执行其中的启动脚本，也未改动导出文件。

- `latest.log` 8968–8974 行：22:33:45，玩家血量 20→7.5；工具箱按已配置的最低生命 8 触发自动下线并停止盾构。
- 8982–8983 行及 `crash-2026-09-06_22.33.46-client.txt`：Meteor `KillAura.onTick` 第 289 行访问已清空的 `mc.player.isAlive()`，抛出 NullPointerException。
- 同目录中午 12:18:55 的旧报告也是同一空指针；不是仅由最新卸货代码或爆炸画面导致的内存错误。
- 对照已安装 `meteor-client-26.1.2-42.jar` 核对了真实 `onTick(TickEvent.Pre)` 签名和下线处理。需要防御世界卸载前后仍可能送达的 tick，而不是关闭低血保护。

## 修复：主包 1.8.16 / 引擎 1.7.12

- 主包增加可选的 `MeteorKillAuraWorldGuardMixin`。只在 Minecraft、玩家、世界或 gameMode 不可用时取消这一轮 KillAura tick；正常世界仍执行原功能，不吞其它异常、不修改 Meteor 文件或设置。Meteor 不存在时由 `@Pseudo` 跳过目标。
- 工具箱的排队下线和引擎的主动下线使用 Minecraft 26.1.2 的 `schedule()`，避免 `execute()` 在客户端线程内同步执行、途中卸载世界。
- 排队操作绑定请求时的世界与连接对象；如果执行前已断线或重连，则放弃旧请求，不误断新会话。新挖掘会话清理可能被丢弃的旧离线标志。
- 最低生命阈值、挂机自动下线、Meteor 自动攻击开关与个人配置均保持原样。

## 验证与安装

- JDK 25 离线 `test jar verifyRuntimeEngineJar` 通过，358 项测试，零失败。新增空对象组合、卸载后晚到 tick、重连会话保护、实际 Minecraft 队列延迟执行和兼容注入声明检查。
- 核对了编译后 HEAD/cancellable 注入、真实 Meteor 目标签名，以及 Mixin 简化回调参数的支持；补丁类和配置仅在主包内，运行引擎不包含 Mixin/宿主辅助类。
- 这不是一次服务器内爆炸实测。完整启动后仍需确认兼容注入实际加载和正常下线表现。
- 安装 `mods/twob2tkit-1.8.16.jar` 与运行引擎 1.7.12；旧主包 1.8.11、引擎 1.7.11 备份到 `config/twob2tkit/backups/2026-09-06-crash-guard`。
- 必须完整重启 Minecraft；只执行 `/twob2tkit reload` 不会加载新增兼容补丁。
