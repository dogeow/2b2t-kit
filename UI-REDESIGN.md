# UI 统一改版 · 主包 1.9.2 / 引擎 1.7.20

范围：全部功能入口、配置页、列表/详情、区域工程与提示交互。保持 Minecraft 原生风格为默认；不改挖矿、飞行、战斗算法，不重置用户配置或工程进度。

1.9.2 同步区域挖“浅层水平／深层竖井”的说明；对应引擎 1.7.20 算法改进及测试见 `AREA-MINING.md`。以下保留 UI 改版的范围与历次记录。

## 1.9.1：功能入口与底栏限宽

- 功能入口使用居中紧凑网格：可用内容宽度达到 452 GUI 单位时分两列，否则单列；单卡最多 260，功能按钮最多 234，收藏按钮紧挨在右侧。只有一个搜索结果时居中显示单卡。
- 首页和各功能配置/列表/确认/按键/配方页的底部动作最多 150 GUI 单位，整组居中；区域工程底部两行动作使用相同规则。字体、按钮高度、收藏/仓库列表、输入框和维度筛选宽度不缩小。
- 滚动高度按网格行数重算，搜索、收藏和窗口缩放后自动限界。原有打开功能、收藏与停止行为不变，运行引擎不变。
- 单元测试覆盖宽/窄窗口、两列切换边界、零/单个/奇数/大量结果、收藏按钮与说明边界、底栏居中，以及生产代码对新布局方法的调用。
- 526 项离线测试通过（新增 9 项）；`test jar verifyRuntimeEngineJar --offline` 通过。已安装 `mods/twob2tkit-1.9.1.jar`，SHA-256 为 `747ee3a907a9bfb2aa6034e8f32036cc0d92bc547c837ea65cfb83f0ede1ee5e`，与构建产物一致。
- 旧主包 1.9.0 备份在 `config/twob2tkit/backups/2026-09-08-ui-compact/`；外部和内置引擎仍为原 1.7.19，哈希未变。需要完整重启，实机新布局尚待确认。

## 实施清单

- [x] 统一七类导航与功能目录，旧入口兼容，风格共用内容。
- [x] 通用设置页：原生控件、滚动、字段验证、持久草稿、停止不受无效输入阻挡。
- [x] 高频工作流：分模式挖掘、区域工程、巡航、风景、收藏。
- [x] 生产、保护、设置与其他功能迁移。
- [x] 列表和详情：搜索与滚动保留、完整内容、编辑与执行分离、破坏性动作确认。
- [x] 全部离线测试、主包/引擎校验、安装与哈希；实机验收单独记录，不以编译代替。

## 交互契约

返回/Esc 保存输入草稿，不执行游戏动作；完整验证后才应用数值。普通开关即时保存并标注，运行时危险设置不得悄悄改任务。开始和停止分离，停止始终优先。无恢复能力不显示“继续”。输入不触发功能热键；紧急停止例外。列表占主要空间，编辑才展开表单。保留配置和原生风格选择；此次主包 UI 发布需要完整重启。

菜单内启动功能使用明确按钮，B/U 等功能键不绕过当前页面的验证或启动上次模式；返回游戏后仍使用原快捷键。开关即时保存，数值/记录退出暂存，点应用或保存才提交。临时记录模型与真实已保存数据分开，取消覆盖对话框也保留输入；成功新建/保存才清除对应编辑草稿。

## 代码入口

- `UiFeature` 是 31 个功能的唯一导航目录，包含七个分类和旧名称搜索别名；`KitWorkspaceScreen` 提供首页、搜索、常用和最近功能。`ClickGuiScreen` 是兼容外观入口，共用同一工作区。
- `KitFormScreen` 提供原生设置/编辑组件、完整校验、隐藏字段错误定位、运行中锁定和停止优先；`UiDraft` 保存原始输入与已应用基线，`UiPageLayout` 统一几何。
- `KitCollectionScreen` 提供可裁剪原生列表、搜索、维度/状态过滤、稳定选中项、滚动恢复、图标及点击时重新验证动作可用性。
- `ClickGuiPages` 的新入口定义生产、挖掘、出行、保护和设置页；`KitRecordPages`、`KitPreparationPages`、`KitRecipePages` 定义记录、物资与配方工作流。
- 区域工程页保留经测试的 `AreaDrafts`，重排为范围/安全/运行入口；运行时不能覆盖执行范围。结构搜索仍调用原 `refreshHits`，只在明确搜索时执行，不在翻看结果时重复计算。
- 原 Screen 构造器保留兼容转发，旧控件代码暂保留用于对照；新入口不进入旧布局。后续修改以以上共享组件为准，不再给旧布局增加独立功能。

## 实机验收（需重启到 1.9.0 后）

- [ ] 首页七类、搜索“盾构/挖树/行动指南”、常用星标、两种配色。
- [ ] 区域 A→关菜单→走到 B→重开补点；草稿、覆盖确认、黄框显示/隐藏、运行时保护。
- [ ] 收藏新增/编辑/填入当前位置→关闭→重开；取消覆盖不丢输入；只有点“前往”才飞。
- [ ] 列表筛选、滚动到中部、查看详情返回、删除取消；小窗口及不同 GUI 缩放无重叠/越界点击。
- [ ] 无效数值仍可停止；菜单里输入字母不启动功能，退出后快捷键及 Meteor 手动飞行正常。
- [ ] 清单选用与查看分离、数量编辑、背包物品选择、配方材料递进、结构搜索与备注、按键重置。

离线测试覆盖逻辑和事件接线，不等同于上述游戏内视觉/点击验收。没有主动重启用户正在运行的多人游戏。

## 1.9.0 发布验证

- 517 项测试通过（原 487 项 + 30 项 UI 回归），零失败/错误；捆绑 JDK 25 离线 `test jar verifyRuntimeEngineJar` 通过。
- 已安装 `mods/twob2tkit-1.9.0.jar`；主包 SHA-256：`5003fa0c2022340201732ac536f66a419246edb9fa810501ad48e3ff8c676832`，与构建产物一致。
- 外部和内置运行引擎均保持 1.7.19，SHA-256 仍为 `433b17bda359e918f6941863c38f98c9c62b0f67a4afba90274198267ed3fa59`；未替换外部引擎。
- 旧主包备份于 `config/twob2tkit/backups/2026-09-08-ui-redesign/twob2tkit-1.8.25.jar`，可恢复。mods 内只保留一个工具箱主包。安装时未修改真实游戏配置、收藏和工程数据。
- 需要完整重启 Minecraft；`/twob2tkit reload` 仅更新引擎，不会更新 UI。重启后的实机验收尚未完成。
