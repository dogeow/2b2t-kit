package dev.twob2tkit;

/** Compact appearance shares the same feature catalog and native page interactions. */
final class ClickGuiScreen extends KitWorkspaceScreen {
    ClickGuiScreen(KitConfig config, KitController controller) { super(config, controller); }
}
