# 区域挖检查与修复（引擎 1.7.20；主包 1.9.2）

1.9.0 的区域工程 UI、草稿/覆盖确认和统一菜单改版见 `UI-REDESIGN.md`；以下保留既往引擎及区域逻辑记录。

## 1.7.20：浅层水平清挖

- 实际高度按最高 Y－最低 Y＋1 计算，1–6 层优先水平，7 层以上保留竖井流程。同 Y 的旧选区仍先按“挖到世界最低层”解析，不能误当作一层。由运行时根据解析后的边界自动选择，不依赖静态 GUI 文案或 Meteor 开关名。
- 先复核实际剩余体积。如果底部已有安全入口，直接接入；否则先开入口井。保持最低目标 Y 附近的作业面，先清邻格头部、脚部，再清上方剩余方块，整列连续两次确认为空后水平进入该格，触发原有补光检查并接近掉落物。常规无障碍浅层区域不再每格返顶；结束后原地进行全体积复核，保留当前位置，不为返顶开穿屋顶。
- 使用实际 `blockInteractionRange` 对应的 `BorerAim.visibleHit` 判断当前可挖目标，执行攻击时仍以真实 `clipView` 和触及距离授权。够不到时采用原安全换位/竖向入口处理该列，不按“装了长臂猿”推定服务器允许超距挖掘。竖向回退也会在射线距离不足时先安全靠近。
- 沿蛇形顺序推进；跨已清空格子使用有界底部路线，保护列挡路时可先清相邻可达格绕过。底部不连通、基岩或净空不足时安全越障换列。浅层模式连同其入口/回退挖掘目标都限制在区域内部，不为换位破坏选区外屋顶或地板；无法安全通行则明确停下。位置移动继续使用原单轴输入和身体扫掠碰撞检查。
- 水、岩浆、容器、封水预留仍受保护；侧水先调用原封堵事务，不能封则保留隔水列。不把保留列计为挖空。存箱返回、战斗/进食暂停、意外落水后的逃生保留原所有者；已保留的水列不会在逃生后立即再次下潜。工具保护/耗尽策略不变。
- 保持检查点 schema 1 和宿主 API 2；模式由实际高度重新选择，恢复仍重新扫描世界，不盲信旧完成记录。保存的工程、A/B 与物资配置不迁移、不重置。
- 日志增加 `strategy=SHALLOW_HORIZONTAL/VERTICAL_SHAFT`、实际层数与 `breakReach`，以及水平绕行/距离回退原因。`HORIZONTAL` 阶段与旧 `DIG` 入口/回退阶段可区分。
- 3 层、24 列的理想动作模拟：最终版水平 608 tick，原竖井 1607 tick；垂直移动命令分别 11 和 553 次。这是离线无网络、无怪物的模拟，不是服务器速度保证。测试校验逐格到位，不等同于实机所有掉落实体都已入包。
- 功能在运行包内，可用 `/twob2tkit reload` 热加载；主包 1.9.2 只同步界面说明，文字需下次完整启动后更新。没有新增 Mixin 或更改公共接口。
- 捆绑 JDK 25 离线 `test jar verifyRuntimeEngineJar` 通过，549 项测试、零失败/错误（新增 23 项）；包括 30×30×3 全部 2700 方块、900 个落脚格无重复遗漏的模拟，以及真实生产入口和安全所有者接线。
- 安装主包 `mods/twob2tkit-1.9.2.jar`，SHA-256 `198f85b0e4c45f383c9b57c4b910d4e7413e4972c0307b74e5e4b432a115ae61`；运行包 `config/twob2tkit/runtime/twob2tkit-engine.jar`，SHA-256 `adbeb764f96943204dcf30af2d11504c02197056a8deed8a388a0824ce629b3d`，内外引擎一致。
- 旧主包 1.9.1 与引擎 1.7.19 备份于 `config/twob2tkit/backups/2026-09-08-shallow-horizontal/`。安装未覆盖配置、工程和检查点。窗口工具对 Minecraft 名称及实际 Java 标识均返回 Invalid app，未能代执行 reload；安装检查时 `twob2tkit-engine.loaded.jar` 仍为 1.7.19，因此须用户执行指令后再开始，不能宣称已实机验证新策略。

## 1.8.25：分两次标 A/B 与独立草稿

- 原区域页的取点仅改输入框，退出后新页面从已应用配置重建；而保存工程先要求两个有效角点，因此无法先留 A 再走到 B。现在取点立即写入独立 `borerAreaDraft`，页面关闭（Esc、返回、热键、进入子页面）也保存名称、A/B 原文和尺寸输入。
- 「保存草稿 / 工程」：只有 A 或只有 B 时保存当前草稿，提示补哪个点；两点完整且校验通过后仍保存正式工程。空点、非法输入或超大范围不能用于预览/开挖；未写完的原文可以在退出时保留，不把它写入引擎使用的坐标。
- 草稿不改变当前工程、区域进度或运行模式。按服务器/维度和已应用区域签名检查恢复；显式加载工程（包括同一工程）、命令改点或清除选区时废弃旧草稿，旧页面关闭时不能将它复活。
- 草稿保存在区域设置页，不伪装成可运行的工程条目。管理页遇到未应用角点会提示返回补齐，并禁止把旧的已应用区域误存为新工程。
- 回归覆盖真实临时目录存盘/重新读取、A/B 任意顺序、未完成文本、窗口重建、新旧编辑实例、工程重载、旧配置兼容和世界隔离；界面接线检查不等于服务器内点击验收。
- 487 项离线测试通过（新增 16 项），`jar`、`verifyRuntimeEngineJar` 通过；仅更新主包，运行引擎保持 1.7.19 且哈希不变。完整重启 Minecraft 生效，不修改真实游戏中的原有工程或配置来代替验收。

## 1.7.19 / 1.8.23：独立隐藏黄框与完成后自动收起

- 旧的 `BorerPreview` 在停止后仍根据 `borerLastMode=AREA` 每帧重画“已标区域”和黄框，导致完成后无法取消。现在仅在明确预览或正在区域挖时显示；停止、完成、重载、重新启动游戏后，保存过的 A/B 和上次模式不会自行恢复显示。
- 区域设置页顶部固定提供“显示黄框 / 隐藏黄框”，位于滚动内容之外。隐藏不应用输入框草稿、不清除 A/B 或工程绑定、不删除工程/进度；挖掘期间也只隐藏显示。活动中重新显示不会重新读取范围或重置当前计划。
- 修正 `dismissAreaPreview()`：只取消显示和预览确认，不再清空区域边界、路径目标或调用 `area.reset()`。防止纯显示操作干扰正在挖掘的区域控制器。
- 命令 `/twob2tkit area hide`、`area show` 对应隐藏/显示。原 `area clear` 是另一个操作：清除当前 A/B 选择和工程绑定，保留已保存工程与磁盘进度；运行中拒绝清除并提示先停止或改用 hide。
- 显示状态是临时预览，不随工程持久化。之后需要查看范围时再次点击“显示黄框”，或点击“应用并查看黄框”查看新输入的坐标；重新开始区域挖也会显示当前范围。
- 460 项回归测试通过，新增 6 项涵盖旧 AREA 配置不自动显示、真实引擎隐藏前后计划/坐标/目标不变、运行中重新显示无重置、渲染模式保护，以及停止/完成/渲染的生产接线。界面布局尚未实机确认。
- 部署主包 1.8.23 与引擎 1.7.19；旧主包 1.8.21 和引擎 1.7.18 备份至 `config/twob2tkit/backups/2026-09-07-area-outline/`。没有更改游戏里的选区、已保存工程或检查点。
- 当前旧黄框可先用 `/twob2tkit reload` 加载引擎 1.7.19 收起，不必清掉工程；新按钮与 hide/show 指令需要完整重启后才可用。宿主 API 仍为 2。

## 1.7.15：跨深度找旧箱、保存容量、井底放箱

- 实际配置为 XZ 16×16、Y -59 到 -50，箱记录仍有旧工程 Y1 的箱子。旧搜索只覆盖最高 Y 附近 12 格 / 人物附近数格，且 `worksiteChest` 拒绝最高 Y＋8 以上的箱子；记录键包含完整 XYZ 两角，改挖更深一段也无法沿用旧箱的满箱状态。这是配置与代码证据，不把旧运行日志当成本次实机复现。
- 在同一 XZ 工地及水平 6 格边界内枚举已加载区块的方块实体，覆盖全部世界 Y，不再受当前挖掘切片高度限制，也不强制加载未知区块。普通已有箱优先；不取箱内物品，不搜索水平远处存储。
- 箱记录改以服务器、维度和精确 XZ 范围共享；兼容读取同一工地下旧的 XYZ 记录，保留旧记录及其它工程。确认为满的箱子不反复开，双箱两半都记录。新工程换 Y 后仍沿用这些记录。
- 在箱内没有空槽但有可合并堆叠时，保存可接受的普通产物种类；先存其它能合并的矿物，不因第一个圆石堆放不下而误报整箱满。恰好存满且本批已清空时，也在关箱重开复核成功后记录容量。
- 旧箱检查不限前 32 个候选。从侧面或下方选择实际可见、够得到且有完整身体净空的操作位置；头顶悬空箱可以从下方开，不要求穿过箱子或站在箱子上。预检去返路线，优先当前高度附近的直达路径。
- 没有可用已有箱才使用背包普通箱。检查人物所在高度、井底、工程顶部、旧箱高度附近的实心支撑；水平优先边缘，再检查内部，不再隔一格搜索或只允许顶部放箱。箱盖、支撑、正常放置射线和放箱后的返回路线都需有效；禁止堵死唯一通路、合并进相邻未知箱或在液体上强放。启用丢石料时仍要求区域外站位。
- 在选定挖掘列内放箱会保留该列；未发现安全支撑/路线或没有备用箱时提示具体原因，保留矿物和工程。新增 `area-cargo-search/choice/reject/capacity/search-failed`，分别记录搜索范围、旧箱/新址、拒绝原因、满箱与合并容量、未加载区块及支撑数。
- 2026-09-07 捆绑 JDK 25 离线 `test jar verifyRuntimeEngineJar` 通过，419 项测试、0 失败，新增 19 项覆盖旧深度记录兼容、服务器/工地隔离、双箱满状态、部分堆叠、井底候选、头顶箱子往返与生产选择顺序。服务器内找箱、放箱和存箱仍待实机验证。
- 仅部署运行引擎 1.7.15，旧 1.7.14 备份在 `config/twob2tkit/backups/2026-09-07-cargo-height/`。主包仍为 1.8.16 / API 2，源码主包版本 1.8.19；不覆盖用户的工地坐标、箱子记录或 Meteor 设置。执行 `/twob2tkit reload`，确认版本 1.7.15 后继续。

## 1.7.14：按实际高度换列、遇基岩继续

- 旧逻辑的换列高度和新列游标固定为区域最高 Y。现在先从下一列的实际剩余方块取得顶层，再扫描同一条 X→Z 路线经过的各列，按其中最高障碍确定净空高度；不先返回整个区域顶部才检查下一列。
- 保持“原列升降、横向单轴移动”的碰撞检查，下一列较低时只升到其表面上方约 1.25 格。当前位置已经更高则不先在原井无谓下降，抵达新列后再下降到实际顶层。中间有更高的存储列/障碍时升高越过，不横挖箱子或未开挖列。
- 下一列已手动挖空时，两次确认后在起飞前跳过。路线未加载则等待；横移前复查，如果服务器回填导致障碍增高，就重新调整高度。区域外进场、超过 128 格的换列和最终返程保留原高位路线。
- 基岩独立于通用保护方块分类。向下挖遇基岩时保留基岩及其下方不可达部分，标记该列“基岩止挖”，继续下一列；箱子、门框、受限方块等仍保持保护，基岩挡住头顶出口时也不会强穿。
- 两轮扫描确认只有暴露基岩及其下方方块的列无需再次下井。中断/热加载后从实际世界重建基岩止挖记录，检查点格式不变；最终复核仍检查基岩上方是否回填，不将整列直接视为空气。
- 结束信息区分“挖空”“基岩止挖”“挡水/存储保留”。新增 `area-transfer-height`、`area-bedrock-columns`，诊断包含 `transferY` 和基岩列数。
- 2026-09-07 捆绑 JDK 25 离线 `test jar verifyRuntimeEngineJar` 通过：400 项测试、0 失败，比原基线增加 17 项。覆盖低/高相邻列、跨越中间高障碍、未知区块、动态回填、基岩高低不齐、恢复与最终复核，以及 8 组高低地形的自然下落/延迟输入碰撞模拟。仍不是服务器实机验收。
- 只安装热更新引擎 1.7.14，旧 1.7.13 备份至 `config/twob2tkit/backups/2026-09-07-area-transfer-bedrock/`；保留已安装主包 1.8.16 和 API 2。`/twob2tkit reload` 生效，确认聊天显示 1.7.14。源码主包版本为 1.8.18，本次不替换主包。

## 1.7.11：工地已有箱子与卸货路线预检

- 21:31:39 / 21:32:34 / 21:32:40 日志均选中墙外新址 `(760864,0,797811)`，而非用户图中已有箱子。旧版只使用自动放过的箱子，且没有预检路线就固定 X→Z 移动，导致相同 SOLID 堵点反复停止。
- 用户明确希望使用旁边箱子：现在优先搜索本工地范围内的普通箱子（含手动放置、双箱），不去远处搜其它存储，不取箱内物品。已够得到且真实射线命中时原地存，无需先升空或站到箱子上方。
- 不够近则选箱子侧面的可用站位，检查箱盖、视线和完整身体净空；不再要求箱子上方有足够空间让人悬浮。开箱瞄准真实可见面，不固定瞄整格顶端。
- 选定旧箱或新放箱位置前，预检去程和返程，尝试 X/Z 两种顺序与可用高度；返程逆序走已验证通道。不会将液体、未加载或保护方块当作通路。动态堵住时换候选并记录真实堵点，不反复选择同一个位置。
- 没有可用箱子才使用背包备用箱；除区域外的平台，也允许已清空的顶部边沿安全位置。占用开挖列/顶部通路的箱子会保留该列，不计为已挖完；箱满时大箱两半一起标记，再找另一箱或另放。
- 若启用了丢石料，丢弃站位仍必须在区域外，不为了就近开箱而在矿区内扔石头。常见单开自动存箱的配置不再被“外侧 4 格”限制。
- 延续物品白名单、整叠备用材料、工具/补给保留、单向存入和关箱重开核对；不中途改用户的工程坐标。未触及固定主机/UI/API。
- 352 项离线测试通过，新增 7 项覆盖截图位置就地存、L 形去返路线及真实输入、低顶侧面存箱、墙外假候选拒绝、危险/未加载路径、工地范围与存储列保护。实机存箱仍待热加载后验证。
- 仅安装运行引擎 1.7.11，旧 1.7.10 备份于 `config/twob2tkit/backups/2026-09-06-nearby-cargo`。`/twob2tkit reload` 生效，无需重启。

## 1.7.10：区域侧水封堵（仅热更新引擎）

- 证据：`borer.log` 19:48:06–19:48:56，1.7.8 在顶部 TRANSFER 清理 `760879 2 797797` 时反复因“通行路线遇水，保留挡水块”停止；此路径此前没有接入放置封水。实际配置 `borerSealLiquids=true`。
- 生产状态机新增 `SEAL_WATER`：进场、返顶、顶部横移、正常下挖发现可封侧水时，先保持原挡块不挖，悬停并释放动作键，优先取背包现有封堵材料填侧水格，再继续原通道。
- 仅封与挡块同高、水平相邻、可达且不占人物身体列的自由水格，不替换水浸容器或其它方块。检测到岩浆、仅头顶/脚下水、不可达或未开启封水时，保留原安全退让行为；不为封水潜入大水体。
- 复用正常 Minecraft 放置接口和现有隔空放置兼容路径；点击支撑面时排除容器、工作台和非完整碰撞支撑，避免为了封水误打开箱子/活板门。不会先破坏干燥挡块再让水涌进来。
- 每个水格最多 3 次放置尝试、90 tick 等待；观察到预期实心块稳定后才继续，不把交互返回成功当成完成。连续同一挡块反复封堵有总次数上限。网络限制/材料不足会明确提示并保留挡块。
- 挡水位置随区域检查点保存；观察到放置方块便记录保护预留，中断/重载后不重新挖掉。侧封进入计划竖井或其顶部通路时保留该列、不计挖完，避免之后拆掉挡水石再进水。区域外侧封不影响完成列计数。
- 日志：`area-water-start/place/confirmed/failed`、`area-liquid-unsealable`，包含原挡块、水格、尝试次数、失败类型与距离，便于区分水、岩浆和放置限制。
- 新增 9 项回归：TRANSFER 封水→挖干挡块→继续走、DIG 不跳过当前井、岩浆/不可封回退、停止重载保护、预测回滚、放置超时/次数上限、材料/距离/人体保护。原 336 项测试仍保留，合计 345 项通过；不等同于服务器实机封堵验收。
- 安装引擎 1.7.10，主包不变；旧运行包备份在 `config/twob2tkit/backups/2026-09-06-area-water`。执行 `/twob2tkit reload`，确认聊天显示 1.7.10，再继续区域挖，无需重启。

## 1.7.7 快满卸货（主机 1.8.11，需重启）

- 区域挖页直接增加「到区域外丢弃普通石料」「自动放箱并集中存入产物」两个开关，均默认关闭，可组合。背包 36 格剩余不超过 3 个空格时触发，集中处理后回原井；不为每一叠单独往返。
- 石料使用明确的 Minecraft ID 白名单：石头、圆石、花岗岩、闪长岩、安山岩、深板岩/深板岩圆石、凝灰岩、方解石、下界岩、玄武岩、黑石、末地石等。青金石、金、铁、钻石、红石等矿物绝不走 THROW。命名/附魔/自定义数据物品排除。至少保留 64 普通建材和 16 煤，按整叠保留，所以可能稍多。
- 卸货站只选区域水平边界外至少 4 格、顶部附近已有坚实支撑的空位。先原井上升、再高处按 X/Z 分开飞；到站后下降至可操作高度，丢弃方向继续朝区域外。外侧受阻、流体或无安全平台则停止提示，不横向挖穿区域外的建筑，也不在挖掘体积内放箱。
- 带普通箱子，自动放置；不搜用陌生箱子，只复用按服务器/维度/精确区域记录的本工程箱子。位置记录于 `config/twob2tkit/area-depots.json`，箱满标记后另放备用箱；没有箱子或没有新位置则停挖，不丢矿物。聊天记录新箱坐标。
- 存入常见挖矿产物；工具、武器、箭、食物、桶、火把、备用箱、保留建材/煤不存。库中物品不往外取。存箱前校验本次打开的 ChestMenu，鼠标有物品、菜单改变、存放不成功均停止；仅自动打开的箱子界面可绕过一般暂停逻辑。
- 存放使用原版 QUICK_MOVE；双端变化只用于短暂等待，不把客户端预测当服务器确认。按 26.1.2 本地源码确认：正确预测的点击不保证回复新 stateId。每批完成/换箱前关闭并重新打开箱子，从服务器新菜单核对实际背包和箱子内容，不通过故意发送错误状态号强制同步。
- 卸货过程暂停 Meteor InventoryTweaks/AutoReplenish，防止 auto-drop/补货抢物品；本工具箱 ContainerAssistant 也暂停自动取货。停止时恢复模块、清理自己打开的容器。遇怪暂停并继续保护；批量存放被中断后需重新开箱核对。箱子本身不会自动拆除。
- 不移动或改写区域工程设置，挖掘状态机在卸货期间冻结；仅执行卸货往返，回到出发列顶部后交回原挖掘状态。停止/热加载后仍按 1.7.6 的实际剩余方块扫描恢复。
- JDK 25 离线 `test jar verifyRuntimeEngineJar`：310 测试通过，零失败。新增石料/贵重物保护、整叠预留、往返实际输入、侧墙/液体/保护块阻挡、箱记录隔离与持久化、模块恢复、预测回滚/重开箱校验等。尚未完成服务器实机放箱/存取验收。
- 发布主包 1.8.11 与引擎 1.7.7；备份目录 `config/twob2tkit/backups/2026-09-06-area-cargo`。新增设置与宿主容器协调，必须完整重启 Minecraft，不能只 reload。

## 1.7.6 中断后异地继续与空列跳过

- 运行证据：2026-09-06 `latest.log` 7815–7822 行，13:39:20 已完成 45/256 列，返顶时按键停止；7860–7865 行，13:41:41 在另一列 Y=5 重开，旧恢复逻辑因位置不同丢弃检查点，完成数归零。随后多个已空列仍 DIG→RETURN，逐层确认空气后再上升。
- 恢复仍绑定服务器/维度/精确区域，但不再要求人站在原坐标；当前位置只决定如何安全进场。旧记录与已挖完的记录均按真实世界重新复核。
- 每次开始/继续先原地分批扫描整个区域，两轮独立观察都为空才计为空列；每 tick 最多读取 2048 格。30³ 共 54000 次读取、28 tick 的逻辑扫描预算。区块未加载、水、保护方块均不会当空气。
- 从当前 XZ 选择最近的未完成列；若当前就在尚未挖完的开口竖井内，直接继续脚下剩余部分。已经到底的空井不会为了确认再次下潜。
- 顶部换列前再检查目标列，期间被手动挖空的也跳过。已确认空列不触发井底火把步骤；真正挖完到达井底仍补光并返顶。
- 最终复查若发现普通回填/残留，重新扫描并继续挖；未加载等待，保护块停止，保留隔水列独立计数。
- 日志新增 `area-v4-survey` 和 `emptyVerified`，记录空列数量、选中列及重新规划原因。未覆盖用户的区域配置或实际工程检查点。
- JDK 25 离线 test、jar、verifyRuntimeEngineJar 全通过，292 项测试、零失败。新增 13 项：DIG/RETURN 异地恢复、大片空井、最低层残留、整体已空、误空气、区块未加载、手工挖空、回填、扫描预算等。新版服务器内实机表现仍待热加载后验收。
- 仅部署 `runtime/twob2tkit-engine.jar`（1.7.6），主包保留 1.8.9；执行 `/twob2tkit reload` 生效。旧引擎备份在 `config/twob2tkit/backups/2026-09-06-area-resume`。

## 1.7.3 速度与使用改进

- 已由 1.7.2 实机日志确认：`RETURN` 阶段 UP/WAIT 每拍交替，因为水平居中检查也要求竖直速度为零。现已只检查水平速度；远处连续上升，距顶部 4 格开始减速，预测上一拍纵向运动避免过冲。
- NoFall 已开时，正常 DIG 关闭 Meteor Flight，保持向下视角、真实命中攻击和自然重力下落。不会每挖一格就转水平并精确悬停。NoFall 未开则保留受控下降；空洞的目标底部有单独制动。
- 确认要挖的方块会放出水/岩浆时保留该隔离块，记录跳过本列并原井返顶。跳过列独立计数，最终不会说全体积已清空。意外进入水/岩浆时暂停挖掘、启飞向上脱离；上方堵塞或超时明确交还控制。
- 到底检测当前光照，低于 8 且背包有普通火把时尝试放置。优先地面，备用墙面；最多两次、12 tick。失败或没有火把不无限等待。已有火把作为照明保留。
- 每秒及停止时保存 `config/twob2tkit/area-progress.json`，绑定服务器/单人存档路径、维度、精确两角。1.7.6 起不再受同一列/原位置限制，先复核实际剩余部分，再从当前 XYZ 继续或前往未完成列。
- 主机 1.8.7 扩充行动指南「出门挖矿」，在按 B/界面启动时读取同一份可编辑清单并提醒，运行中检查短缺变化。旧自定义数量保留，新增条目只迁移一次。物资不足不拦开挖。
- GUI 仍归固定主机，未引入界面热加载；物资提醒需重启主机一次。挖掘/恢复/补光逻辑可用 `/twob2tkit reload` 热加载引擎 1.7.3。

## 发现的问题

旧实现的蛇形排序不是主要故障。区域挖经由通用盾构 tick 执行，仍会被找矿的走近、换目标、下落、脱困及战斗追赶分支提前接管。居中计算还同时使用旧 yaw、通道朝向和重新计算的井心朝向，按键方向与实际视角不一致。

9 月 4 日的轮转日志有可复核证据：`2026-09-04-4.log.gz` 解压后第 780–790 行，1.6.298 在 `POSITION_TOP` 从 X382819.520 漂到 X382825.700，再持续卡墙。旧测试仅测策略布尔值，没有运行这条执行链。

其它问题：可见面已验证后又改瞄中心；破坏无进度超时只覆盖找矿；临时 suppression/液体风险被误当作空方块；侧面漏水不断抢占；最后一列在井底提前停机；加载新工程后可能继续显示旧预览。

## 当前执行方式

`DefaultTunnelBorerEngine.tick` 在进入通用导航前把 AREA 交给专用 `BorerAreaRunner`。安全保护只暂停挖掘和移动，不调用找矿或战斗追赶导航。区域动作不会调用旧 `BorerAreaThink` 或自动修改代码。

生产 `BorerAreaPlan.step` 负责完整状态：

1. 原地扫描实际剩余方块；保留区域记录，跳过两轮确认全空的列。
2. 当前井还有剩余且上方已清空则就地继续；否则检查最近未完成列与途经列的实际高度，从当前列升至安全换列高度，一次只沿 X 或 Z 平移。
3. 对准 1×1 列，从本列最高剩余方块 Y 逐层向下。在真实可挖距离内直接向下挖，不逐层校准精确悬停高度。
4. 每层实际观察到两次空气后再下移游标；不把裂纹等级或预计秒挖当作完成。
5. 清掉最低 Y 的方块并下降到该层后，或遇基岩止挖后，按蛇形顺序选择下一未完成列，检查实际高度再换列，跳过已空列。
6. 最后一列返回区域顶部，然后分批复查整个体积。普通回填会重新规划；未加载等待、非基岩保护块停止；基岩止挖独立计数，不会误报整区域已清空。

`BorerAreaMotion` 把同一拍动作变成固定 yaw，水平与垂直输入分开；受控下挖可同时挖和下降，自然下挖使用重力。`BorerAreaRunner` 重施这一拍的精确视角；挖掘沿真正验证过的射线，命中错误立刻松攻击，不挖邻列也不向前乱走。移动/射线/服务器破坏无进展均有独立计时及明确坐标。

`BorerAreaFlightSession` 临时使用 Meteor Velocity 飞行并限制速度；结束时恢复会话仍拥有的设置，不覆盖用户期间手改的值。人在空中结束会保留飞行并提示接管，避免返顶后跌回深井。会识别 Scaffold、AntiAFK 的运动/旋转输入冲突并显示具体原因。没有反复模拟 C 键、瞬移或另发破坏包。

## 范围与限制

- X、Z、Y 两端均包含。例如最高角 `(100, 65, 200)` 与另一角 `(129, 36, 229)` 是 30×30×30，即 900 列、27000 格。
- 两点 Y 相同沿用既有“向下到世界底部”语义，不代表 1 层。
- 用户自己设置挖掘范围，更新不会覆盖成测试用的 30³。
- 正常下挖仅当前列及选定 Y 范围；进场、返顶、顶部平移可能清理必要通行空间。容器/刷怪笼会停止；脚下基岩止挖后继续下一列，液体保留隔离并跳过，均不计为完整清空。
- 暂停界面/战斗不换列；显式停止/热加载通过匹配检查点及实际方块恢复。改变服务器、维度或两角不盲用旧工程；移动到其它井不会丢弃同一工程。
- 体素模拟不能验证服务器反作弊、实际网络延迟、Meteor 插件额外输入和全部非完整方块形状。实机整区完成需在游戏里另行验收。

## 验证

1.7.3 历史验证：2026-09-06，JDK 25 执行 `test jar verifyRuntimeEngineJar --offline`，262 项测试全部通过，运行引擎 API 2 结构校验通过。新增自然重力模拟、64 格返顶性能预算、边挖边下、隔水跳列、检查点序列化及恢复、物资迁移、光照条件和自然下挖/逃生 Flight 转换测试。

现有 JUnit 运行生产状态机和生产按键映射，不另写测试专用状态机。四角分别执行完整 30³，检查 108000 次实际模拟破坏的唯一性、逐层顺序、相邻列、900 次到底/返顶、独立 0.6×1.8 碰撞反馈。另测天然洞穴、原已挖空、服务器延迟/假空气、回填、未加载、保护块/液体、漂移、高空进场、输入延迟和 Flight 设置恢复。

历史 1.7.3 发布曾安装主机 1.8.7，旧主机 1.8.5、旧引擎 1.7.2 备份于 `config/twob2tkit/backups/2026-09-06-speed/`。当前安装版本见本文顶部，不将离线逻辑测试当成实机整区完成。
