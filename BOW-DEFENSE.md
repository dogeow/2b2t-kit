# 骷髅反击：主包 1.9.3 / 引擎 1.7.21

## 修复范围

- 区域挖反击：根据远端怪物逐 tick 的实际位置采样估速，不再假定 `deltaMovement` 足够可靠。停止两拍清掉提前量；反向横移和急转弯立即更新方向；瞬移、过期样本和世界时间重置不产生巨大的预测速度。
- 共享弓箭解算：按当前游戏的箭速、每 tick 阻力/重力、箭生成高度及发射者附加速度求截击。加入最多 4 tick 的延迟补偿；用同一飞行时刻的移动身体碰撞箱验证，不把将来的位置当成一直静止的目标。
- 移动预测遇墙停止对应分量，可沿墙滑动；箭穿过固体、液体或其它生物的路径不授权射击。命中校验使用实际身体，不依赖原版随箭龄增加的额外碰撞宽容。急变走位、服务器回拉和随机散布仍可能导致未命中，不能保证百发百中。
- 放箭前重新解算、写入玩家朝向，并确认最近可见相机帧已朝向该预判点；真正 `releaseUsingItem` 前再次校验。使用正常旋转包同步同一朝向，再让原版释放弓箭，不使用独立于可见瞄准的静默旋转。
- 相机最终对齐只在本工具箱持有战斗视角、第一人称且未开启 Freecam 时生效。最近画面最多允许 2 tick、6 度跟踪差，兼容近处小白横移。第三人称/Freecam 下不会自动释放本功能持有的弓箭；切回第一人称继续。不改变用户视角模式设置。
- 挖矿反击的有效视角优先于残留的恶魂瞄准；恶魂防护遇到非自己持有的弓时清理旧瞄准。拉弓期间持续协调 KillAura/BowAimbot/BowSpam，受伤保护不会重新抢开 KillAura；AutoLog 保留。
- 无主动威胁不反击、苦力怕优先、吃东西让出按键、近战、恢复挖矿、物资保护均保留。手动弓箭、食物和没有持有拉弓状态的其它功能不经过自动放箭限制。

## 主包与兼容

`BowAim`、相机/放箭 Mixin、宿主桥接都属于固定主包，所以这次必须完整重启 Minecraft，仅 reload 不够。稳定 API 新增有默认实现的能力查询与回调，API 版本仍为 2。新引擎运行于旧宿主时不冒险射箭，提示重启并先防御；旧引擎配新宿主不改变手动释放行为。

## 证据与验证边界

- 本次读取实际 `latest.log` 到第 9536 行、`borer.log` 到第 809 行。19:49 的记录是引擎 **1.7.19** 对同一骷髅反复放箭，不能当成 1.7.21 的实机结果。当时已加载缓存也是 1.7.19，磁盘待加载引擎为 1.7.20；后续新伤害日志为“空闲”，未出现新版实机射击记录。
- 只读 Meteor 的已保存 modules.nbt：KillAura 开启；BowAimbot/BowSpam/FreeLook/Freecam 未开启。文件值不等于此刻内存中的实时设置；未修改该配置文件。
- 新增纯逻辑弹道、采样和可见帧测试，以及 ASM 接线检查。弹道测试独立按小时间步推进箭与移动目标并检查实际身体相交；接线检查覆盖当前 26.1.2 方法签名、放箭前复算、可见相机、手动使用绕过、旧宿主和威胁筛选。
- 版本验证：捆绑 JDK 25，`test jar verifyRuntimeEngineJar --offline` 通过，共 579 项测试（新增 30 项）；不启动测试游戏，不修改真实存档/配置。离线测试与 Mixin 方法签名检查不等于运行中 Mixin 已应用，也不等于服务器命中率验收。
- 新诊断：`area-defense-wait-view` 记录目标、期望/相机朝向；实际 `area-defense-shot` 记录 visible、飞行 tick、估算速度和预测位置。不会在仅松开按键时就提前记为已放箭。

## 安装记录

已安装 `mods/twob2tkit-1.9.3.jar` 和 `config/twob2tkit/runtime/twob2tkit-engine.jar`（1.7.21），与构建产物 SHA-256 一致。mods 中仅一个工具箱主包；旧主包 1.9.2 与引擎 1.7.20 备份于 `config/twob2tkit/backups/2026-09-08-bow-prediction/`，可以恢复。未重启或控制正在运行的游戏，未替换 loaded.jar 缓存。

- 主包 SHA-256：`81f0ca68ee73098c658f06d6acfe310f7f16f620c734027482edad9d97bf3744`
- 引擎 SHA-256：`0c7aa3b39f731a271c8cb12d870e6088e88b3e87cae70394e7d38d3f52cf212c`
