# 自动找矿改进：引擎 1.7.8

## 依据与边界

用户说明这次尚未运行自动找矿。当前日志只有区域挖，不能当成找矿复现。历史证据使用 `/Applications/.minecraft/logs/2026-09-04-4.log.gz` 解压后的行号：1606–1724 行反复 `skip-side-wall`，身体列 Z=311120 而支撑列 Z=311119；1651 行因下行无落脚转入 skip-ore。11462–11503 行可见 1.6.299 的混合挖掘/拾取过程，但不据此宣称 1.7.8 已实测。

本次只改 runtime/engine 的找矿/拾取导航及策略。区域挖、放箱卸货、菜单、主机 API 未改；安装只换运行包，主机仍为 1.8.11。

## 修改

- 寻路/找矿通道和垂直开路的选块原点统一使用身体列，支撑方块列仍留给真实站立安全判断。避免坑沿跨列导致挡路块被当成侧墙；仅 ORE 使用新导航原点，其它模式沿用原值。
- 普通找矿行走：侧偏超过碰撞余量时先横移回中，不同时按 W 挤墙。居中时朝向锁在通道轴，移动 tick 的朝向会在 Meteor 写视角后重施；不覆盖攻击中的真实准星。
- 新增有界开放通道寻路，水平半径 12、最多扩展 640 个站立点。只走已加载、脚/头净空、脚下安全的格子，四方向路径不切对角墙角。路径代价包含层差；有现成通路优先走，否则退回原开路算法。不是全地图/所有矿脉的全局最优规划。
- 向上步进仅使用已有 Step 能力及头顶净空；下落遵守 NoFall 落差上限，局部路径最多探 12 格深，更大高差继续交给原垂直开路。每个路点重验身体碰撞，变化/卡住时清理路径重规划。火、岩浆、水、岩浆块、细雪、蜘蛛网、浆果丛不作为开放通路。
- 空闲拾取检查所有勾选矿种，不再限于 lastMinedOre，也支持重开后补捡混合矿脉残留。每 10 tick 扫描一次；煤/石英经验模式仍排除相应掉落物。
- 下落掉落物的向下搜索范围随安全落差扩展，NoFall 时可追踪落到原先 10 格范围以下的矿物；向上仍保持原 10 格，避免扩大追逐高空物品。
- 同一掉落目标加入距离容差，避免两堆物品距离轻微变化就切换。理论拾取箱内等 20 tick 仍没捡到就继续靠近；头顶堵塞时不再触发坑沿盲跳。
- 拾取超时改为无进展计时：接近、实际裂纹推进/挡块变空气、到达路线节点、背包增长均算进展，不再无论进展如何到 200 tick 就放弃。真正连续 160 tick 无进展才暂缓，100 tick 后允许重试；未拾取的暂缓实体不报告成“已全部捡完”。
- 容量判断按实际掉落物的物品类型和组件判断可堆叠性，包括副手；钻石矿石的空余容量不等于钻石的容量。拾取结束释放所有移动键，避免上一拍按键残留。
- 拾取开路允许在主矿目标已消失时继续，不再被 `no-ore-goal` 清掉刚选的遮挡块。

## 验证与日志

JDK 25 离线 `test jar verifyRuntimeEngineJar`。326 项回归通过，新增 16 项：路线最短长度、拐角和两高障碍、L 形通道的生产方向输入及 0.6 宽身体反馈、扫描预算、落差/低顶/危险格、身体列 vs 支撑列、持续 1200 tick 开路进展、绕路进展、不前进跳跃超时、物品堆叠、目标稳定、NoFall 掉落扫描等。既有区域挖/存箱测试仍通过。

新增 `ore-walk-route` / `ore-walk-replan`、`loot-start` / `loot-progress` / `loot-defer` / `loot-finish` 到 borer.log，记录目标、节点、当前位置、无进展计时及暂缓原因。

验证是离线构建和逻辑/体素测试，不代表已验证服务器延迟、掉落物所有权、Meteor 额外输入或实际整段自动找矿。热加载 `/twob2tkit reload` 后再启用自动找矿验证；不要把 AREA 日志归因到 ORE。

运行包部署到 `/Applications/.minecraft/config/twob2tkit/runtime/twob2tkit-engine.jar`，旧 1.7.7 备份在 `config/twob2tkit/backups/2026-09-06-ore-navigation/`。
