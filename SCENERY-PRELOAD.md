# 风景预加载（优先 Bobby）

已安装主包 **1.8.21** / 引擎 **1.7.18**，Minecraft 26.1.2、Java 25。

## 1.7.18：修复始终等待光照、覆盖数为零

- 实机 03:23:29–03:25:29 的 1.7.17 日志显示 Bobby 已连接、服务器视距 8 / 客户端 32 / 扫描 8，但一直停在 `0/13130`、`pending=0`。不是 Bobby 初始化失败，也不能靠增大视距解决。
- 原因是把 `ChunkPos.pack()` 当成 `LevelLightEngine.lightOnInColumn(long)` 的参数。Minecraft 的 `LightEngine.setLightEnabled(ChunkPos, boolean)` 实际登记 `SectionPos.getZeroNode(chunkX, chunkZ)`，两种 long 编码不同。在截图坐标 `(760873, 797799)` 对应的 `(47554, 49862)` 区块，旧查询会错误返回 false。
- 修正为统一的光照列编码，保留真实区块判定、10 tick 稳定确认和 Bobby 写入/读回校验；未启用光照的区块仍等待，不直接绕过安全判断。
- 将等待提示拆成缺真实区块、只有旧缓存、光照未到齐、稳定观察和后端未接收。新增 `scenery-capture`，记录目标区块、对象类型、真实状态、光照列键、光照状态、稳定 tick 和后端待处理数。
- 新增 3 项使用 Minecraft **真实 LevelLightEngine** 的测试（初始化注册表但不启动游戏）：复现截图坐标旧查询失败、新查询成功，验证正负/原点坐标与邻列隔离；另加 1 项生产接线回归。全量 454 项测试、离线打包及 API 2 引擎校验通过，实机仍待重载确认。
- 仅安装引擎 1.7.18，主包不动；旧 1.7.17 备份至 `config/twob2tkit/backups/2026-09-07-scenery-light/`。源码主包版本同步为 1.8.22。本次执行 `/twob2tkit reload` 后点“继续上次”，原圆心、1024 格半径和缓存记录不改，无需重启。

## 1.7.17：Mac 的 Bobby 缓存与服务器视距

- 实机日志 2026-09-07 02:31:26：Voxy 自行报告 `Voxy is unsupported on your system`；该实例为 Apple M3 Pro、OpenGL 4.1。上一版只检查 API、没有先查图形兼容性，故误给出通用“未初始化”。本版不绕过 Voxy 图形检查，而是优先复用已安装并启用的 Bobby 5.2.13。
- 直接读取 `ClientPacketListener.serverChunkRadius`，即服务器区块缓存半径包的状态；不拿客户端显示视距或模拟距离冒充。客户端 32、服务端 8 时扫描预算按 8；服务器下调后即时更新。数据不足仍等待真实包，不按理论视距整片填满完成记录。
- Bobby 的展示用 `FakeChunk` 不参与覆盖计数，也不能作为飞行净空证据。只接收当前世界的真实区块，原生光照启用且稳定 10 tick 后保存。
- 使用 Bobby 原生 `FakeChunkManager.save`，不执行它返回的“安装假区块” Supplier。等其单线程保存队列中的序列化任务完成，再异步同步区域存储、读回本批各区块；坐标、记录时间、完整状态、光照和区段字段全部有效才确认。失败、旧缓存或缺块不计成功，不在游戏线程阻塞等待。
- 每批最多 32 区块，写入和读回期间暂停继续提交。Bobby 实例、世界缓存切换或写入失败则停飞保留进度，不混用缓存。保留 Bobby 的渲染距离、清理规则与个人配置；若设置为每次退出清缓存，会提示先调整而不空跑。
- 当前本机显示视距为 32 区块（约 512 格）；跑图缓存半径不等于能同时显示的半径，不自动扩大显示设置，也不宣称 Bobby 具备 Voxy 的低精度超远景渲染。
- 原有主包 1.8.20 已有入口，所以执行 `/twob2tkit reload` 即可使用引擎 1.7.17 的 Bobby 模式；旧界面标题可能仍写 Voxy，运行状态会明确显示 Bobby。主包 1.8.21 的中性标题和说明在下次完整重启后生效。

## 使用

打开工具箱的 **巡航 → 风景预加载**。原生 Minecraft 菜单与模块栏都提供入口；首次从 1.8.20 以前的主包升级需要完整重启。

- 半径单位为方块，16–4096，默认 256；提供 128 / 256 / 512 / 1024 快选。第一次建议从露天处试 128 格，大半径会增加耗时与缓存占用。
- **当前位置新建**：记录当前 XZ 为圆心，替换上次风景任务的进度，不改变巡航目标或挖矿工程。
- **继续上次**：沿用上次圆心、半径与确认进度；不要求人站在原位置。不混用其它服务器、维度或不同缓存后端的记录。
- **停止并保留**：暂停并保存已确认批次。开启菜单会悬停，关菜单继续；紧急停止与既有挂机低血量/陌生玩家保护继续有效。
- 完成后返回圆心上空，不自动降落；保留 Meteor Flight 供你手动接管，并恢复借用前仍归本功能所有的飞行设置。

也可使用：

```text
/twob2tkit scenery
/twob2tkit scenery start 256
/twob2tkit scenery resume
/twob2tkit scenery stop
/twob2tkit scenery status
```

## 覆盖与记录

- 对与圆形范围相交的每个 16×16 区块建立清单，包括圆边缘、负坐标与跨区块边界的格子。顺序从中心向外按方形螺旋展开；已实际加载的周围区块直接纳入，飞往剩余漏块，不强求每一格都由人物踩过。
- 只读取客户端当前真实加载的完整区块，不强制加载区块，不修改服务端视距。区块光照已启用、同一对象稳定至少 10 tick 后，才提交缓存。
- 当前优先使用 Bobby 原生序列化/存储机制，参见[官方说明](https://github.com/Johni0702/bobby)。没有安装 Bobby 时才使用 Voxy 接口；Voxy 本身仍须满足图形环境要求，不强制初始化。
- 最多 32 个区块一批，只有确认成功的批次才提交进度。Bobby 等待完整写入与读回；Voxy 后备模式等待接收、保存队列与执行中任务归零；确认状态还需稳定 20 tick。
- 进度在 `config/twob2tkit/scenery-progress.json`。停止/断线/热更新前保存已确认部分；未完成后台处理的批次不会持久化为完成，继续时重新检查。清空缓存或更换后端后应选“当前位置新建”，不要复用旧确认记录。
- `config/twob2tkit/borer.log` 中的 `scenery-start/state/confirmed/stop/error` 记录覆盖与失败原因。HUD 区块数包括本批已接收、尚待后台完成的部分；最终结束必须全部确认。

## 飞行与边界

- 临时使用现有已验证的 Meteor Flight Velocity 会话，不模拟 C 键，不修改原巡航坐标。活动互斥，不同时运行挖矿、建造或其它导航。
- 从至少 Y160 或地表以上约 48 格飞行；探测前方约 12 格与两侧地表，遇山先停止前进并升高。高度只升不反复下降，正常地形不需要飞到极高处。最大控制高度为 Y512。
- 真实身体碰撞、区块加载、世界边界均检查。头顶堵住时不会挖掉建筑或强穿，需要移到露天位置再继续；不挖矿、不放方块、不切物品栏、不抢 Meteor 自动吃的右键。
- 无法收到区块、缓存模块停止、服务器拒绝移动或路线被挡会明确暂停/停止并保留漏块，不虚报 100%。接收/地形等待最多两分钟，移动连续无进展最多 30 秒，可稍后继续。
- 离地距离降低靠近地面骷髅的风险，但不保证对所有怪物、投射物或服务器机制免疫；既有安全下线不关闭。

## 验证与安装

- 捆绑 JDK 25 离线 `test jar verifyRuntimeEngineJar`：450 项测试通过。1.7.16 的 436 项基线上新增 14 项，覆盖 Bobby 优先选择、原生 NBT、异步保存/同步/读回顺序、旧/缺/错区块拒绝、全批确认、服务器 8/客户端 32 与动态下调、真实区块接线。
- 核对了当前 Bobby 5.2.13 的单线程保存队列、存储方法、NBT 字段和真实/假区块类型；Minecraft 26.1.2 下服务器字段和客户端区块光照门控均以实际 JAR 为准。缓存模块不兼容时只停此功能，不影响挖矿。
- 引擎仍要求宿主 API 2，不增加主包接口；旧 1.8.20 可热加载 Bobby 功能。主包文案更新在完整重启后生效。
- 部署主包 `mods/twob2tkit-1.8.21.jar` 与引擎 1.7.17；旧主包 1.8.20、引擎 1.7.16 备份在 `config/twob2tkit/backups/2026-09-07-bobby/`。
- 尚未实机验证服务器限速与 Bobby 最终远景显示；离线覆盖模拟和构建通过不等同于游戏里跑完。未自动启动实机飞行。
