# 界面与区域挖反击（主机 1.8.9 / 引擎 1.7.5）

## 1.7.9：只响应主动威胁（主包不变）

- 用户要求：只是看见、未接近也未攻击的怪不要打，不打断挖矿。1.7.8 的范围/视线筛选缺少交战条件；现增加共享 `BorerEngagement`，同时用于主动射击、附近怪停挖、苦力怕保护和找矿避怪，不再从另一分支重新触发停挖。
- 客户端不一定获得服务器的完整仇恨目标；以可观察证据判断：近处明确针对玩家、怪物自身持续朝玩家靠近、正朝玩家做出攻击姿态、射来的弹射物、最近真实怪物伤害。已知目标是别人时忽略（实际伤害或临近爆炸除外）。
- 每 5 tick 比较怪物自身位移，而非双方距离缩短；玩家下挖/飞过去不等于怪物走过来。普通接近限制在 10 格内、纵向差不超过 3 格；射手已攻击/正在射击不要求走近。
- 苦力怕单纯可见或静止贴近不自动射击；正在接近或实际膨胀才防御。膨胀的危险范围按普通 6 格/充能 12 格处理，远处膨胀不触发无关反击。
- 同级威胁仍保持苦力怕→射手→其它的原有顺序；无主动威胁即取消拉弓、恢复原物品栏并继续挖矿。攻击姿态保留短暂 30 tick 信号，兼容射手两次射击的间隔。
- 日志增加 `area-defense-ignore-passive`、`area-defense-resume-work`；目标日志标明 engaged/recentAttacker。验证 336 项离线测试通过，新增 10 项覆盖闲置/其它目标、玩家自身移动、接近/远离、隔层、射手、来袭弹射物和临近爆炸。
- 此规则约束工具箱行为；不修改用户另行开启的 Meteor 自动攻击配置。实际服务器表现仍需热加载后验证。只部署引擎 1.7.9，旧包备份于 `config/twob2tkit/backups/2026-09-06-reactive-defense`；`/twob2tkit reload` 生效。

## 地点收藏（主包 1.8.24）

- 默认收起坐标编辑表单，列表使用主要空间；顶部「新增 / 编辑」展开，行内「编辑」载入该地点并展开，保存成功后收起。收起、切维度和调整窗口保留当前未保存输入。
- 搜索与展开按钮同一行，维度筛选一行；列表延伸到返回按钮上方，整体宽度随窗口调整，最大 640 GUI 像素。600×360 GUI 下由约 3 行增加到 7 行（维度标题也占一行）。
- 移除名称固定 16 字截断；按实际剩余像素裁剪整行摘要，编辑按钮提示完整名称和坐标。四个行内动作仍保留。窗口过矮且展开编辑时隐藏放不下的列表，可收起编辑恢复。
- 不修改收藏内容、巡航或挖矿引擎。本页在固定主包，需完整重启；仅热加载引擎不会更新界面。几何与控件接线回归测试不等于实机视觉验收。

## 操作约定

- GUI 热键遵守 `clickGui`：默认 Minecraft 原生分页；设置页可立即切换模块面板，面板设置页可切回原生分页。保留并尊重已有选择，不在配置迁移中强制打开面板。
- 六类入口、全局功能搜索、记住上次分类。输入关键词后回车进入匹配功能的配置页，不直接启动动作。
- 功能名称打开配置，右侧明确显示开始/停止；配置页底部同样用动作文字，启动成功自动关闭界面。
- 总览始终提供停止全部。Esc 返回上一层；输入框内 Esc 取消本次编辑。紧急停止在搜索、数值编辑中仍有效。
- 公共详情行增大，背景、状态色和说明统一。所有继承 KitHudScreen 的独立页面共用背景和热键规则。
- 数值错误显示在当前页面且保留输入；编辑框内输入 U/B 等字母不会意外关闭界面或启动功能。
- 列表不接受裁剪区外隐藏行的点击；窗口可缩到当前屏幕以内。独立的旧详情页继续保留业务设置，统一主入口负责导航。

## 区域挖

区域页不再分“标点/断面/工程”三层。命名、两角、脚下/准星取点、长 X/宽 Z/高 Y、预览、物资和安全入口在同一滚动内容区；保存工程、加载/管理工程、开始/继续、返回固定可见。

保存先校验两角，再一次写入，避免只改了 A、B 却仍是旧值。30×30×30 由尺寸直接生成，包含上下端点。加载另一工程后刷新字段；没有换工程时重建布局保留坐标草稿。区域挖实际固定 1×1，已移除不生效的条带宽/每次高度入口，工程摘要也显示 1×1。

预览调用独立 previewArea，不再用 toggle；即使存在断点也不会因为点击“查看黄框”而误启动挖掘。

## 主动反击

新增默认开启的自动反击，可在安全页关闭。区域挖发现可见敌对生物时原地暂停挖掘，不追出竖井：

1. 苦力怕优先。
2. 持弓或弩的射手，其中特别优先最近五秒攻击过玩家的生物。
3. 近处其它敌怪，包括近战骷髅、蜘蛛等。

同级目标距离小幅变化时保持当前目标，避免拉弓不断被切换打断。近距离用武器反击；远处从背包选弓，使用原有 BowAim 的弹道与地形校验，拉满后松右键放箭。没有弓箭或射线被挡时明确举盾提示。弩作为敌方射手识别；当前自动远程武器使用弓。

临时协调原先已开的 KillAura/BowAimbot/BowSpam，避免它们在拉弓期间抢槽位、视角或提前放箭，结束时恢复。共用弹道解算新增非敌对实体遮挡检查，避免向友方/玩家遮挡的弹道放箭。

停止、打开界面、切换挖掘模式或回家时取消拉弓并恢复物品栏选择；战斗结束后继续原区域进度。日志为 area-defense-target / area-defense-shot / area-defense-blocked。

## 验证与生效

### 1.8.9 / 1.7.5 菜单与手动飞行修复

- 用户原配置 `clickGui=false`；1.8.8 的打开入口却无条件进入 ClickGuiScreen。已恢复分流及双向切换。
- 实际 `/Applications/.minecraft/meteor-client/modules.nbt` 的 Flight 为 Velocity、speed=0、no-sneak=true；AntiAFK/Scaffold 未启用。区域悬停临时零速度被持久保存，是手动空格不起飞的直接原因。
- 首次进入世界/加载引擎时恢复遗留备份。旧版无备份的上述零速组合修复为 Meteor 默认 0.1，不改变非零手动速度，不主动开关 Flight。
- 临时写速度前原子保存原速、上次值和请求值到 `area-flight-speed.bak`。停止恢复速度后暂留凭据，兼容 Meteor 在停止前已保存临时速度；重启只恢复与凭据相符的值，检测到用户另改速度则保留用户值。
- 断线和退出时停止盾构并恢复临时设置。异常退出仍可通过备份恢复最后写入附近的速度状态；无法匹配备份的值不猜测覆盖。无备份旧版的原始手动速度不可追溯，因此零速迁移采用默认值。
- JDK 25 离线构建、运行包结构校验及 279 项测试通过；新增 9 项覆盖遗留零速、异常丢失会话、停止前持久化、外部改速、备份失败等。菜单开关做了静态入口检查，尚未完成新版实机点击/飞行验收。
- 安装目标：`mods/twob2tkit-1.8.9.jar` 与运行引擎 1.7.5；备份目录 `config/twob2tkit/backups/2026-09-06-menu-flight`。菜单和退出钩子属于主机，必须完整重启 Minecraft。

### 上版 1.8.8 / 1.7.4 验证记录

JDK 25 离线 test、jar、verifyRuntimeEngineJar 通过，270 项测试，零失败/错误。新增目标抢占、射手稳定选择、最近攻击者、墙后/死亡/非敌对排除、拉满放箭及坐标表单验证。既有 30³、自然下挖、返顶、恢复、补光测试仍通过。

验证包含构建与逻辑测试，不代表已完成所有游戏分辨率的视觉验收或服务器射击实测。

已安装 mods/twob2tkit-1.8.8.jar 和 config/twob2tkit/runtime/twob2tkit-engine.jar（1.7.4），哈希与构建一致。旧版移到 config/twob2tkit/backups/2026-09-06-ui-defense，可恢复。本次含固定主机界面和共享弓箭桥接，需完整重启 Minecraft；仅 reload 引擎不够。
